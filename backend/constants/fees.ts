/** Base tuition fee used to compute the balance due when an application is approved (no per-program fee schedule yet, so kept as one shared constant — mirrors frontend/app/constants/fees.ts). */
export const BASE_TUITION_FEE = 1000;
